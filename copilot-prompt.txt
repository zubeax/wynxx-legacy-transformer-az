generate an ibm enterprise cobol program that creates the ecb payments statistics file as specified in the pdf document 
Regulation ECB 2020/59 amending Regulation ECB 2013/43 on Payment Statistics.

generate the ddl for a db2 database that contains all entities and fields required to create the statistic
and add embeded sql statements in the generated cobol program to read them.
generate sample load datasets with plausible transaction data for the db2 tables.
there should be at least 1000 transactions distributed over the categories from the statistic.
the cobol program should read and aggregate the data, then format them into the specified output dataset
with the cobol xml generate feature.

the input, processing and formatting functionality should be implemented in separate subroutines
that are invoked with the call statement.
generate build and execute jcl for the program.
generate a README.md markdown file documenting all aspects (including building and executing) of the program.

collect all generated files in a .zip archive ready for download.